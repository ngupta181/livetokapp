import 'package:flutter/cupertino.dart';

class ColorRes {
  // TikTok brand colors
  static const colorPrimary = Color(0xFF000000); // TikTok primary black
  static const colorPrimaryDark = Color(0xFF121212); // TikTok dark background
  static const colorIcon = Color(0xFF25F4EE); // TikTok cyan
  static const colorTheme = Color(0xFF25F4EE); // TikTok cyan theme
  static const colorPink = Color(0xFFfe2c55); // TikTok pink/red
  static const black = Color(0xFF000000); // Pure black
  static const colorTextLight = Color(0xFF898989); // TikTok text gray
  static const colorLight = Color(0xFFF1F1F1); // TikTok light gray
  static const greyShade100 = Color(0xFFF8F8F8); // TikTok background gray
  static const white = Color(0xFFFFFFFF); // Pure white
  static const red = Color(0xFFfe2c55); // TikTok red (same as pink)
}
